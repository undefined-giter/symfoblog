{% extends 'base.html.twig' %}

{% block title %}{{ user.userProfile.name ?? user.email|split('@')|first|split('.')|first|split('_')|first|capitalize }} Follows{% endblock %}

{% block bread %}
    {{ user.userProfile.name ?? user.email|split('@')|first|split('.')|first|split('_')|first|capitalize }} Follows
{% endblock %}

{% block body %}
    {{ include ('profile/_header.html.twig') }}
    
    {{ include ('profile/_tabs.html.twig') }}

    {{ include ('follower/_follower_list.html.twig', {users: user.follows}) }}
{% endblock %}
